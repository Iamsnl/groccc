const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { await prisma.product.create({ data: { name: 'Test', slug: 'test-' + Date.now(), description: 'Test', price: 10, stock: 10, categoryId: 'cmmjieks', images: '[]', isFeatured: false, isTrending: false } }); console.log('success'); } catch(e) { console.error('ERROR:', e.message); } finally { await prisma.$disconnect(); } } main();
